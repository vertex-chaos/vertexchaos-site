---
import BaseLayout from "../layouts/BaseLayout.astro";
import { site } from "../config/site";
import Card from "../components/Card.astro";
import Tag from "../components/Tag.astro";

const title = "vertex_chaos";
---

<BaseLayout title={title} description={site.subhead}>
  <div class="flex items-center gap-2 text-sm text-slate-500">
    <span class="text-slate-400">●</span>
    <span>{site.domainLabel}</span>
  </div>

  <section class="mt-10">
    <h1 class="whitespace-pre-line text-5xl font-semibold tracking-tight text-slate-900 sm:text-6xl">
      {site.headline}
    </h1>
    <p class="mt-6 max-w-2xl text-base leading-7 text-slate-600">
      {site.subhead}
    </p>

    <div class="mt-8 flex flex-wrap gap-3">
      <a
        href={site.links.email}
        class="rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white no-underline hover:bg-slate-800"
      >
        Email me
      </a>

      <a
        href={site.links.portfolio}
        class="rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-semibold text-slate-900 no-underline hover:bg-slate-50"
      >
        Portfolio
      </a>

      <a
        href={site.links.github}
        class="rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-semibold text-slate-900 no-underline hover:bg-slate-50"
      >
        GitHub
      </a>
    </div>
  </section>

  <section class="mt-16">
    <h2 class="text-2xl font-semibold text-slate-900">What I help with</h2>

    <div class="mt-6 grid gap-6 lg:grid-cols-3">
      <div class="grid gap-6 lg:col-span-2 lg:grid-cols-2">
        <Card
          title="Automation and tooling"
          body="Turn multi-hour chores into a button. Or a scheduled job. Same vibe."
        >
          <div class="flex flex-wrap gap-2">
            <Tag label="PowerShell" />
            <Tag label="Python" />
            <Tag label="CI/CD" />
          </div>
        </Card>

        <Card
          title="AD, DNS, upgrades, hardening"
          body="Because identity breaks in creative ways."
        >
          <div class="flex flex-wrap gap-2">
            <Tag label="Windows" />
            <Tag label="Identity" />
          </div>
        </Card>

        <Card
          title="Architecture and migrations"
          body="Make it stable, then make it cheaper. In that order."
        >
          <div class="flex flex-wrap gap-2">
            <Tag label="Infra" />
            <Tag label="Cloud" />
          </div>
        </Card>
      </div>

      <aside class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h3 class="text-sm font-semibold text-slate-900">Quick links</h3>
        <div class="mt-3 grid gap-2 text-sm">
          <a class="no-underline text-slate-700 hover:text-slate-900" href={site.links.portfolio}>Portfolio</a>
          <a class="no-underline text-slate-700 hover:text-slate-900" href={site.links.github}>GitHub</a>
        </div>

        <div class="my-6 border-t border-slate-200"></div>

        <h3 class="text-sm font-semibold text-slate-900">Get in touch</h3>
        <a
          href={site.links.contact}
          class="mt-3 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white no-underline hover:bg-slate-800"
        >
          Contact for quote
        </a>
        <p class="mt-3 text-xs leading-5 text-slate-500">
          Rate depends on scope. Send a problem statement and constraints.
        </p>
      </aside>
    </div>
  </section>

  <section class="mt-16">
    <div class="grid gap-6 lg:grid-cols-3">
      <div class="lg:col-span-2 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <div class="flex items-center justify-between">
          <h2 class="text-2xl font-semibold text-slate-900">How I work</h2>
          <span class="text-xs text-slate-500">simple, explicit, testable</span>
        </div>

        <ol class="mt-6 grid gap-3 text-sm text-slate-600">
          <li>1) Define scope</li>
          <li>2) Ship increments</li>
          <li>3) Add guardrails</li>
          <li>4) Document everything</li>
          <li>5) Hand-off</li>
        </ol>

        <p class="mt-6 text-sm text-slate-700">
          If it cannot be repeated, it is not done.
        </p>
      </div>

      <aside class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
        <h3 class="text-sm font-semibold text-slate-900">Get in touch</h3>
        <a
          href={site.links.contact}
          class="mt-3 inline-flex w-full items-center justify-center rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white no-underline hover:bg-slate-800"
        >
          Contact for quote
        </a>
        <p class="mt-3 text-xs leading-5 text-slate-500">
          Rate depends on scope. Send a problem statement and constraints.
        </p>
      </aside>
    </div>
  </section>

  <section class="mt-16">
    <div class="flex items-center justify-between">
      <h2 class="text-2xl font-semibold text-slate-900">Featured work</h2>
      <span class="text-xs text-slate-500">anonymized, on purpose</span>
    </div>

    <div class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
      <div class="flex items-start justify-between gap-6">
        <div>
          <h3 class="text-base font-semibold text-slate-900">
            High-volume Windows file transfer automation
          </h3>
          <p class="mt-2 text-sm text-slate-600">
            Moved large file sets reliably using an input list, with progress reporting and restartable execution.
          </p>

          <div class="mt-4 flex flex-wrap gap-2">
            <Tag label="Client" />
            <Tag label="PowerShell" />
            <Tag label="Windows Server" />
            <Tag label="File Ops" />
          </div>
        </div>

        <a
          href="/work"
          class="mt-1 text-sm font-semibold text-slate-700 no-underline hover:text-slate-900"
          title="View case studies"
        >
          ↗
        </a>
      </div>
    </div>
  </section>

  <footer class="mt-16 flex items-center justify-between border-t border-slate-200 py-8 text-sm text-slate-500">
    <span>© {new Date().getFullYear()} vertex_chaos</span>
    <div class="flex gap-6">
      <a class="no-underline hover:text-slate-900" href="/about">About</a>
      <a class="no-underline hover:text-slate-900" href="/privacy">Privacy</a>
    </div>
  </footer>
</BaseLayout>
